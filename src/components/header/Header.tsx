import { StyledHeader } from "./Header.styles";

const Header = () => {
  return (
    <StyledHeader>
      <div>UI Kit React</div>
      <div>(user options)</div>
    </StyledHeader>
  );
};

export default Header;
